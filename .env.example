# true = process only a small sample of the book (development mode)
# false = process the complete book (production mode)
VITE_DEV_PROCESSING_MODE=true

# Maximum number of pages to process while developing
VITE_DEV_MAX_PAGES=10

# Maximum audio duration (minutes) to process while developing
VITE_DEV_MAX_AUDIO_MINUTES=10
